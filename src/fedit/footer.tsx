import * as React from 'react';
import { FC } from 'react';

interface FooterProps {
}

export const Footer = ({
}: FooterProps) => {

  return (
    <h1 className="p-4 bg-gray-400">Footer.</h1>
    );
};

